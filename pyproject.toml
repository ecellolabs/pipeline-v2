[project]
name = "mmagentic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["atria-core", "docling>=2.127.0"]

[tool.uv.sources]
# Temporarily pointed at the local checkout to test a fix to
# _StoreImagesToFiles (MultiPageDocumentInstance support) before upstreaming.
atria-core = { path = "../atria_core", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agentic"]
